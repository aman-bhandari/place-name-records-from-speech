{
  "name": "place-name-records-from-speech-tools",
  "private": true,
  "description": "Playwright for the interface check (ui/check/ui_check.cjs). The web UI itself lives in ui/.",
  "scripts": { "ui-check": "node ui/check/ui_check.cjs" },
  "devDependencies": { "playwright": "^1.62.1" }
}
