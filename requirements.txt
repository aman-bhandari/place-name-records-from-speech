# Eknaam (UKIS 2026 P-015). Pinned to the versions the results in docs/RESULTS.md were measured with.
# torch: install the build for your hardware first (https://pytorch.org/get-started/locally/), e.g.
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cu130   # NVIDIA, CUDA 13
#   pip install torch==2.14.0                                                       # CPU only (slow build, fine for serve)
torch==2.14.0
transformers==5.17.0
huggingface_hub==1.33.0
numpy==2.5.3
pyarrow==25.0.1
soundfile==0.14.0
librosa==1.0.0
PyYAML==6.0.3
requests==2.34.2
fastapi==0.141.1
uvicorn[standard]==0.54.0
python-multipart==0.0.32
pydantic==2.13.5
httpx==0.28.1
pytest==9.1.1
